services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: gunicorn webCarpentry.wsgi:application --bind 0.0.0.0:8057
    ports:
      - "8057:8057"
    depends_on: []
