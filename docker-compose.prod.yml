services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: gunicorn webCarpentry.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    depends_on: []
